# ── Core scraping ────────────────────────────────────────────────
google-play-scraper>=1.2.7
apple-app-reviews-scraper>=1.0.0

# ── Data processing ──────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0

# ── Language detection ───────────────────────────────────────────
langdetect>=1.0.9

# ── Visualisation (Google_scraper.py analysis plots) ─────────────
matplotlib>=3.7.0
seaborn>=0.12.0
scipy>=1.10.0

# ── Standard library (no install needed, listed for reference) ───
# sqlite3   — built-in
# datetime  — built-in
# time      — built-in
# warnings  — built-in
